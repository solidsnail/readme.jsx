<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 800 150">
  <foreignObject width="100%" height="100%">
    <div xmlns="http://www.w3.org/1999/xhtml">
      <style>
@keyframes dotBounce {
  0% { transform: scale(1); }
  50% { transform: scale(1.5); }
  100% { transform: scale(1); }
}

@keyframes fadeIn {
  0% { opacity: 0; }
  100% { opacity: 1; }
}

#container { font-family: var(--fontStack-sansSerif, -apple-system, BlinkMacSystemFont, "Segoe UI", "Noto Sans", Helvetica, Arial, sans-serif, "Apple Color Emoji", "Segoe UI Emoji"); display: flex; align-items: center; justify-content: center; width: 100%; height: 120px; background: transparent; text-align: center; position: relative; }
#title { font-size: 64px; font-weight: 800; color: #1f2937; letter-spacing: -1px; margin: 0px; animation: fadeIn 0.8s ease-out; position: relative; }
#jsx { font-weight: 300; color: #6b7280; position: absolute; top: 40px; }
#dot { position: absolute; top: 22px; left: 2px; width: 8px; height: 8px; background-color: #3b82f6; border-radius: 50%; animation: dotBounce 2s ease-in-out infinite; }
      </style>
<div id="container">
    <h1 id="title">README<span id="jsx">jsx<span id="dot"></span></span></h1>
</div>
    </div>
  </foreignObject>
</svg>